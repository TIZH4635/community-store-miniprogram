# 部署文档 — 社区商店微信群运营小程序

## 一、代码准备

```bash
cd community-store-miniprogram
git remote add origin <你的仓库URL>
git push -u origin main
```

## 二、微信云托管部署

### 1. 创建环境
- 微信公众平台 → 开发 → 云托管 → 创建环境
- 选择 Python 运行时
- 环境 ID：`prod-d8g39b9mi22fcf335`

### 2. 关联 Git 仓库
- 云托管 → 部署管理 → 关联源码
- 选择 GitHub/GitLab 仓库
- 设置自动构建（push 后自动构建镜像）

### 3. 配置环境变量
在云托管环境变量中添加：
```
DASHSCOPE_API_KEY=***
```

### 4. 构建部署
- 云托管 → 构建镜像 → 部署
- 等待构建完成，查看日志

## 三、小程序配置

### 1. 云环境 ID
- 开发者工具 → 项目设置 → 填云环境 ID：`prod-d8g39b9mi22fcf335`

### 2. API 地址
- 修改 `miniprogram/pages/*/` 下的 API URL 为云托管域名

## 四、验证部署

1. 微信开发者工具 → 预览 → 手机扫码测试
2. 检查功能：
   - 登录（微信授权）
   - 信息发布（文字+照片）
   - AI 识别（粘贴消息 → 分析）
   - 群消息监控（录入+AI分析）
   - 推送时间设置（管理员）

## 五、后续更新

代码推送到 Git 后，云托管自动构建部署。
